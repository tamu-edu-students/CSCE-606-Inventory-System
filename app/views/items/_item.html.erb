<div id="<%= dom_id item %>">
  <p>
    <strong>Name:</strong>
    <%= item.name %>
  </p>

  <p>
    <strong>Description:</strong>
    <%= item.description %>
  </p>

  <p>
    <strong>Created date:</strong>
    <%= item.created_date %>
  </p>

  <p>
    <strong>Value:</strong>
    <%= item.value %>
  </p>

  <p>
    <strong>Bin:</strong>
    <%= item.bin_id %>
  </p>

  <% if item.picture&.image&.attached? %>
    <p>
      <strong>Picture:</strong><br>
      <%= image_tag item.picture.image.variant(resize: "200x200") %>
    </p>
  <% else %>
    <p>No picture uploaded</p>
  <% end %>

</div>
