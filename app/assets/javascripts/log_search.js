// This file is no longer needed since we're using a proper form in the navbar
// The form will handle the submission and parameter passing automatically

document.addEventListener('turbo:load', function() {
  // No custom click handler needed anymore as we're using a proper form with submit button
  // The form action is set to log_history_path and will include the date parameters
});
